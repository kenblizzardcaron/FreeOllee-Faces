*.iml
.gradle
/local.properties
/.idea/caches
/.idea/libraries
/.idea/modules.xml
/.idea/workspace.xml
/.idea/navEditor.xml
/.idea/assetWizardSettings.xml
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties

# Built APKs are published as GitHub Release assets, never committed.
*.apk

# Python venv for the BLE tools in tools/
.venv/

# Signing keystores are NEVER committed; CI gets them from GitHub Secrets.
*.jks
*.keystore

# Internal planning docs, kept local only (not published to the public repo).
docs/superpowers/
